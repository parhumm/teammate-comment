node_modules/
# The whole data directory is local state: the SQLite file, its WAL sidecars,
# and the session signing secret. None of it belongs in the repo.
data/
widget/dist/
.DS_Store
.env
.playwright-mcp/
*.png
